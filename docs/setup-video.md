# Setup video

Full setup walkthrough on YouTube: extract the release zip, run the server, install the mod, add the OBS browser source, and configure layout: all in one video.

**[Watch the setup guide on YouTube](https://youtu.be/AYFSaeBVvkM)**

Also linked from **`/help`** when the server is running.

When updating the URL, change it here and in **`OverlayServer/wwwroot/js/site-links.js`** (`STS2_LINKS.setupVideo`).
